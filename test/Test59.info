Circular: By default, strongly connected components are detected at evaluation time (if separated by a non-circular attribute), and evaluated in topological order. This improves performance (over evaluating as a single component) if evaluation starts in an outer component.