aspect Test31 {

  syn int A.value();
  eq B.value() = 1;
  eq C.value() = 2;

  rewrite B {
    when(value() == 1)
    to A {
      return new C();
    }
  }

  rewrite B {
    when(true)
    to A {
      throw new Error("This should not happen");
    }
  }
}